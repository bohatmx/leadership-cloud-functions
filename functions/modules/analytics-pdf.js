const functions = require("firebase-functions");
const admin = require("firebase-admin");
const PDFDocument = require("pdfkit");

exports.analyticsPDF = functions.https.onRequest((req, res) => {
  // const updateType = req.query.type;
  // res.send(true);
});
